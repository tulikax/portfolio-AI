/** Public path to the CV PDF in `/public` (served at site root). */
export const CV_PUBLIC_PATH = '/Tulika CV folio.pdf' as const

/** Matches Tailwind `md` — keep in sync with responsive nav utilities. */
export const NAV_DESKTOP_MIN_WIDTH_PX = 768
