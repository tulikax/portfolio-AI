/** Mirrors the homepage's section order. Process moved to /about with the long copy. */
export const NAV_LINKS = [
  { label: 'Work', href: '#work' },
  { label: 'Stack', href: '#stack' },
  { label: 'About', href: '#about' },
] as const

export const NAV_EASE_OUT = [0.23, 1, 0.32, 1] as const
